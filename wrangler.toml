# Cloudflare Pages Configuration
# This app uses client-side storage (localStorage via Zustand)
# For production with real-time sync, add D1 database and Workers

name = "friend-challenge"
compatibility_date = "2024-01-01"

# For Cloudflare Pages, deployment is handled via:
# wrangler pages deploy dist

# If you want to add a D1 database for persistent storage:
# [[d1_databases]]
# binding = "DB"
# database_name = "friend-challenge-db"
# database_id = "your-database-id"

